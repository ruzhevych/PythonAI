## Reading

```
python --version
python -m venv .venv
source .venv/bin/activate

python -m pip install --upgrade pip
pip install gTTS
pip install python-dotenv
pip install numpy

python main.py

deactivate

```